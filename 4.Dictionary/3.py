# the dictionary is empty, all() returns True. Else, it returns false
#the dictionary is empty, any() returns False. If at least one key is true
squares = {}
print(all(squares))
print(any(squares))
print(len(squares))
print(sorted(squares))
